generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
  url      = env("DATABASE_URL")
}

enum Platform {
  YouTube
}

model Account {
  id            String
  platform      Platform
  access_token  String
  refresh_token String

  @@id([platform, id])
}
